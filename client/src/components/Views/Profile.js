import React from "react";

function Profile() {
	//Hopefully this container can also contain an update button that toggles a form to update the user's info
	return (
		<div className='profile-wrapper'>
			<h1>Hello this is the profile component</h1>
		</div>
	);
}

export default Profile;
